use std::io;

fn main() {
    // read input
    let mut line = String::new();
    io::stdin().read_line(&mut line).expect("Read error");
    let n: i32 = line.trim().parse().expect("Parse error");

    // construct output
    
}
